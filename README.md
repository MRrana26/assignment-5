<body>
<div>
        <h3>1️⃣ What is the difference between var, let, and const?</h3>
        <h4>Ans: var vs let</h4>
        <ul>
            <li>var is old.</li>
            <li>because use to with problem</li>
            <li>variable name  again declare in var</li>
            <li>can changed var is value</li>
            <br>
            <br>
            <li>let: variable name not again declare</li>
            <li>so our code is clean</li>
            <li>but let value can be changed</li>
        </ul>
        <h4>Ans: const</h4>
        <ul>
            <li>const is professional use</li>
            <li>but need to variable value changed. this time not use const</li>
            <li>because not changed to const value again</li>
            <li>and const is variable not again declare</li>
        </ul>
        </div>
    <div>
        <h3>2️⃣ What is the spread operator (...)?</h3>
        <h4>Ans: Spread...</h4>
        <ul>
            <li>Spread... this use for array or object </li>
            <li>I have value an array [10, 20, 30] and use Spread...</li>
            <li>and result  = 10 20 30 it's means spread out</li>
            <li>also use for Object</li>
        </ul>
  </div>
<div>
        <h3>3️⃣ What is the difference between map(), filter(), and forEach()?</h3>
        <h4>Ans: map(), filter(), and forEach()</h4>
        <ul>
            <li>map(), filter(), and forEach() those use small code means easy our coder life</li>
            <li>map() use an array. this full value can changed</li>
            <li>example: const doubles = numbers.map(num => num * 2) this numbers array can double</li>
            <br>
            <br>
            <li>filter() use to an array value can filtering</li>
            <li>const nums = [1, 2, 4, 5, 6]</li>
            <li>example: const nums = numbers.map(num => num > 2) this give 2 before all value: example: output [4, 5, 6]</li>
            <br>
            <br>
            <li>forEach() means loop</li>
            <li>loop use to more line code writhing but forEach() use just single line</li>
            <li>forEach()  use to small bugs are created.</li>
            <li>forEach() is modern in ES6</li>
        </ul>
   </div>

<div>
        <h3>4️⃣ What is an arrow function?</h3>
        <h4>Ans: arrow function</h4>
        <ul>
            <li>function alternative arrow function</li>
            <li>function use to function name declare need. but no need in arrow function</li>
            <li>Example: function sum(a, b){ <br>
                        return a + b; <br>
                } </li> <br><br>
            <li>arrow function: <br>
            1. const sum = (a, b) => {<br>
            return a + b } <br> <br>
        2. const sum = (a, b) => a + b;</li>
        <br>
        <li>arrow function use in ES6 version</li>
        </ul>
  </div>
    <div>
        <h3>5️⃣ What are template literals?</h3>
        <h4>Ans: template literals</h4>
        <ul>
            <li>This template literals use variable set in string</li>
            <li>example: let name = 'Masud Rahman';
                <br>
                console.log("My Name is: ${name}");
            </li>
            <li>`` this is (backtik) and this powerfull</li>
        </ul>
        </div>
  </body>
  

